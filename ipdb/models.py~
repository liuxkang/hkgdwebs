from django.db import models

# Create your models here.
class Ipdb(models.Model):
    mac_addr = models.CharFiled(max_length=14)
    ip_addr = models.CharFiled(max_length=14)
    dept = models.CharFiled(max_length=20)
    noted = models.CharFiled(max_length=100)
